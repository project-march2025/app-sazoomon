import { Button, Text, View } from 'react-native';
import { useNavigation } from '@react-navigation/native';
import { SafeArea } from '@/components/SafeArea';

export default function EnterInformation() {
  return (
    <SafeArea>
      <View>
        <Text>EnterInformation</Text>
      </View>
    </SafeArea>
  );
}
